Master -  phiên bản ổn định để sự dụng
develope - phiên bản phát triển nếu sử dụng thử ok thì tích hợp vào master
Tác ở đâu ra thì hợp lại vào đó
Tổng hợp chức năng:
    +, Tham chiếu và tag tham chiếu
    +, Đặt ghi chú
    +, Quản lý thời gian
    +, Ẩn hiện folder
    +, Delete folder và file
    +, Tạo folder và file

    +, Đặt nhãn cho file
    +, Drad and Drop (Làm sau)
    +, Thùng rác, phục hồi (Làm sau)
    +, Thông báo - lịch sử(Làm sau)
    +, Tìm kiếm (Làm sau)

Tổng hợp Click chuột:

    Click chuột phải hiện hộp thoại quản lý Time
    Click CT Liên quan, Ghi chú để thêm tag, note => Thêm kiểu input rồi ấn ok
    Click CT để xóa tag, note => Lập trình 1 nút xóa
    Click CT  View/Hident Folder  
    Click CT nút add folder
    Click CT bật link file

    Chức năng chuột phải tất cả đều có:
    Lưu ý hiện tên vùng trên cùng khi bấm chuột phải (this)
        - Delete
        - Rename: Hiện tên cũ vào khung đổi, sử dụng focus
        - Thêm folder (thêm vào vùng click)
        - Thêm file (thêm vào vùng click)
        - Tham chiếu tới

Tổng hợp hộp thoại:
    1, Hộp thoại tạo thư mục đổi tên thư mục
    2, Hộp thoại tạo file đổi tên file
    3, Hộp thoại tạo tag Liên Quan tham chiếu, danh sách để time chiếu
    4, Hộp thoại quản lý time

- Cấu trúc trương trình:

1, box{} - Tất cả mọi thứ liên quan đến box
    + create()
    + offBox()
    + clickOf() - Nút Hủy
    + clickOkBox() - Nút Ok
    + view()

2, time{}
    + timeView()

3, folderTag{}

4, totalFunction{}
    + create{}
        + folder
        + file
    + rename{}
        + folder
        + file
    + Tham chiếu{}
    + Xóa{}

6, mouseRight{}
    + mousePosition()
    + mouseOff()
    + mouseDisabled()
    + list_element_disabled
    + $this_mouse_right
    + click()
